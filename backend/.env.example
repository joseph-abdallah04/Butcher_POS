# --- Cloud SQL (operational DB) ---
# These match the values used by the ETL Cloud Function.
DB_USER=postgres
DB_PASS=12345678Aa_
DB_NAME=ops_store
INSTANCE_CONNECTION_NAME=project-1aeac36d-6e65-4529-9bd:australia-southeast1:butchery-ops-db

# Set to any non-empty value to use private IP (leave empty for public IP)
PRIVATE_IP=

# --- BigQuery (data warehouse for the Reports page) ---
BQ_PROJECT_ID=project-1aeac36d-6e65-4529-9bd
BQ_DATASET=Our_data_warehouse

# --- Optional ---
# Comma-separated list of additional CORS origins for local dev
EXTRA_CORS_ORIGINS=
